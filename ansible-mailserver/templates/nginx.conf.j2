server {
    listen 80;
    server_name {{ nginx_server_name }};

    location / {
        include         uwsgi_params;
        uwsgi_pass      unix:/var/www/django-cyradm/cyradm/mysite.sock;
    }
    location /static {
        alias /var/www/django-cyradm/static;
    }
}
